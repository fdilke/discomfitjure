; startup script for REPL
(ns sprout.repl
  (:use clojure.set
     sprout.monoid sprout.endovec-monoid sprout.endovec
     ; sprout.util
     ; sprout.kazunk.clique
     ; sprout.kazunk.kazunk
     ; sprout.spike
     ; sprout.idempotents
))

(println "Welcome to Discomfitjure")

; (doall (map show-auto (filter goop? (submonoids (all-endos 2)))))



